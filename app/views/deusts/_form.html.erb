


<%= form_for(@deust) do |f| %>
  <% if @deust.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deust.errors.count, "error") %> prohibited this @deust from being saved:</h2>

      <ul>
      <% @deust.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn button-red" %>
  </div>
<% end %>
