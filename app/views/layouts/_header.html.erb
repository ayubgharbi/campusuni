
        <nav class="navbar navbar-default navbar-fixed white no-background bootsnav" style="background-color: transparent !important">
        <!-- Start Top Search -->
        <div class="top-search">
            <div class="container">
                <div class="input-group">
                    <span class="input-group-addon"><i class="fa fa-search"></i></span>
                    <input type="text" class="form-control" placeholder="Search">
                    <span class="input-group-addon close-search"><i class="fa fa-times"></i></span>
                </div>
            </div>
        </div>
        <!-- End Top Search -->

        <div class="container">    
            <!-- Start Atribute Navigation -->
            <div class="attr-nav">
                <ul>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" >
                            <i class="fa fa-book"></i>
                            <span class="badge">3</span>
                        </a>
                        <ul class="dropdown-menu cart-list">
                            <% Post.last(3).each do |post| %>
                                <li>
                                    <a href="#" class="photo"><%= image_tag post.matiere.image.url(:thumb) %></a>
                                    <h6><%= link_to post.name, deust_matiere_post_path(post.deust, post.matiere, post) %></h6>
                                    <p class="m-top-10"><span class="price"><%= post.matiere. name %></span></p>
                                </li>
                            <% end %>
                        </ul>
                    </li>
                    <li class="search"><a href="#"><i class="fa fa-search"></i></a></li>
                    <li class="side-menu"><a href="#"><i class="fa fa-bars"></i></a></li>
                </ul>
            </div>        
            <!-- End Atribute Navigation -->

            <!-- Start Header Navigation -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-menu">
                    <i class="fa fa-bars"></i>
                </button>
                <%= link_to "accueil", root_path, class: "btn btn-md " %>
            </div>
            <!-- End Header Navigation -->

    
        </div>  


        <!-- Start Side Menu -->
        <div class="side">
            <a href="#" class="close-side"><i class="fa fa-times"></i></a>
            <% Deust.all.each do |deust| %>
                <div class="widget">
                    <h6 class="title"><%= link_to deust.name,deust %></h6>
                    <ul class="link">
                        <% deust.matieres.each do |matiere| %>
                            <li><%= link_to matiere.name, deust_matiere_path(deust, matiere) %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>
        </div>
        <!-- End Side Menu -->

    </nav>


    <!--menu-btn-->
    <a href='#' class='menu-btn' >
        <span class='lines' >
            <span class='l1' ></span>
            <span class='l2' ></span>
            <span class='l3' ></span>
        </span>
    </a>
    
    
    <!--menu-->
    <nav class='menu' >
        <ul>
        <li><%= link_to 'Acceuil', root_path %></li>
        <% Deust.all.order("created_at ASC").each do |deust| %>
            <% if deust.name != "Livres" %>
            <li>
                <%= link_to deust.name, deust_path(deust), class: "link" %>
            </li>
            <% end %>
        <% end %>
         <% if session[:user_id] %>
            <li><%= link_to 'logout',   logout_path, method: :delete %></li>
        <% end %>
        </ul>
    </nav>